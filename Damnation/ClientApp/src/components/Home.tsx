import * as React from 'react';
import { connect } from 'react-redux';
import GameSection from './WebPlayer/GameSection';

const Home = () => (
            <GameSection></GameSection>
      



);

export default connect()(Home);
